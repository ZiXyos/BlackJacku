const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const tableSchema = new Schema({
  : {
    type: ,
    required: true
  } 
});

module.exports =  = mongoose.model("", tableSchema);